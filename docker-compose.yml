services:
  backend:
    build: ./backend
    container_name: rachford-backend
    ports:
      - "8000:8000"
    restart: unless-stopped
    
  frontend:
    build: ./frontend
    container_name: rachford-frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
    restart: unless-stopped
